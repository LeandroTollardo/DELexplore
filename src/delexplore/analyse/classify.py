"""Binder classification (orthosteric, allosteric, cryptic).

Classifies enriched compounds based on their enrichment pattern across
selection conditions (protein, protein+inhibitor, blank).
"""

import logging

logger = logging.getLogger(__name__)


def classify_binders() -> None:
    """Classify enriched compounds into binder categories.

    Not yet implemented.
    """
    raise NotImplementedError("binder classification is not yet implemented")
